---
name: update-claude-md
enabled: true
event: stop
action: warn
---

**Reminder: Update CLAUDE.md**

Before finishing, check if you modified any plugin code in `local/` (e.g., `local/learner/`, `local/learnerprogression/`, `local/admindashboard/`, `local/tutors/`, `local/sitebackup/`).

If plugin behaviour, queries, template context, filters, routes, or features changed, you MUST update the corresponding section in `CLAUDE.md` to reflect those changes.

Do NOT finish without updating documentation.
